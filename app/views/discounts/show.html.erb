<%= render partial: "shared/header" %> 
<h4><%= "Buy #{@discount.quantity} get #{@discount.percentage}% off"%></h4>
<%= link_to "Edit Discount", edit_merchant_discount_path(@discount.merchant_id, @discount.id)%>